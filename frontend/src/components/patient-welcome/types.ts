/** Walkthrough lifecycle: a brief establishing beat at reception before the
 * patient can walk, the free-roaming scroll-driven journey itself, and the
 * cinematic exit once Continue is pressed. */
export type ScenePhase = 'entering' | 'walking' | 'exiting'
